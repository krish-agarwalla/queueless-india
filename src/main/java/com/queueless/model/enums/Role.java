package com.queueless.model.enums;

public enum Role {
    SUPER_ADMIN,  // You (The Owner)
    ORG_ADMIN,    // Your Clients (Hospital/Bank Managers)
    CUSTOMER      // The Public (People waiting in line)
}